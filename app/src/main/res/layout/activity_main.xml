<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_soft">

    <FrameLayout
        android:id="@+id/mainContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="108dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="14dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/bg_bottom_nav_shell"
        android:elevation="18dp"
        android:orientation="vertical"
        android:paddingStart="8dp"
        android:paddingTop="6dp"
        android:paddingEnd="8dp"
        android:paddingBottom="8dp">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNav"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:minHeight="70dp"
            android:paddingStart="2dp"
            android:paddingEnd="2dp"
            app:itemActiveIndicatorStyle="@null"
            app:itemBackground="@drawable/bg_bottom_nav_item"
            app:itemIconSize="25dp"
            app:itemIconTint="@color/bottom_nav_colors"
            app:itemPaddingBottom="8dp"
            app:itemPaddingTop="8dp"
            app:itemRippleColor="#1A6366F1"
            app:itemTextAppearanceActive="@style/TextAppearance.VirtualLab.BottomNav.Active"
            app:itemTextAppearanceInactive="@style/TextAppearance.VirtualLab.BottomNav.Inactive"
            app:itemTextColor="@color/bottom_nav_colors"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_nav_menu"/>
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>

